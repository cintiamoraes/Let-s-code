
//criamos a função anônima dentro da variável const que não pode ser alterada, com os parâmetros de entrada e colocamos o return para o resultado desta soma 
const somar = function(a,b){return a+b}

console.log(somar(1,3))